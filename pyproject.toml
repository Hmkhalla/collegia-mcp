[project]
name = "collegia-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Hmkhalla", email = "hassan.mkhallati@excellentium.be" }
]
requires-python = ">=3.13"
dependencies = [
    "fastmcp[apps]>=3.4.7",
]

[project.scripts]
collegia-mcp = "collegia_mcp:main"

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"
